测试小程序
T0  -点灯程序
      GPIO挂载在7f00-7fbf上
T1  -
acia-异步串口通讯程序
     串口通信使用R6551A(R6551支持1M频，R6551A支持2M频)
     R6551A挂载在7FC0-C3上，由于A2、A3没用使用，
     所以#0111 1111 xx00 - #0111 1111 xx11都映射在前4个地址上
     R6551A使用的串口波特率是通过主分频，当为96分频时，在1.842Mhz
     的晶振下，波特率为19200.即波特率=晶振*10^6/分频。分频在命令寄存器
     
 6551-Helloworld    [2021-3-16]
      串口打印Hello World！程序
      遇到操作表出错致使编码出了bug，查半天发现自己在操作表上把BEQ备注为绝对寻址
      实际BEQ是相对寻址，后面跟的是地址偏移量，偏移量=偏移量所在地址到目标地址所在距离
      负偏移是向前跳转，正偏移是向后跳转
lcd1602_helloworld  [2021-3-21]
      lcd1602显示“helloworld”程序
      lcd1602挂载在7f80-7fbf上，通过6522与CPU通信，所以需要先初始化6522才能间接操作
      lcd初始化需要足够的延时确保上一条指令执行完毕，具体延时在lcd主控芯片的手册上有
      
 程序入口地址划分：
reset                   [8000]      复位后pc将指向这里
main 				[8010]	主程序
nmi				[80a0]	不可屏蔽中断
irq				[80b0]	中断响应

text_hello			[8100-811f]

6551_send_byte		[8120]	发送一个字节，a寄存器传入要发送的字节
6551_send_hello		[8130]	发送hello
6551_init			[8140]	6551初始化，8bit无检验1停止位，19200波特率

6522_init			[8200]	gpio初始化
lcd1602_init		[8210]	初始化 8bit 2lines 5*8 有光标，不闪烁
lcd1602_write_c		[8240]	写命令	a寄存器传入要写入的命令
lcd1602_write_d		[8260]	写数据	a寄存器传入要写入的数据
delay_ns			[8280]	delay_ns 必须给寄存器A赋值后才可调用此函数
lcd1602_hello		[82a0]	lcd1602显示hello
lcd1602_checkBusy		[82c0]	检查lcd是否在忙
delay_us			[82f0]	delay_us    约延时300毫秒
delay_ms			[8300]	delay_ms    约延时1秒
